[workspace]
members = ["view"]

[package]
name = "tang"
version = "0.1.0"
edition = "2024"

[features]
default = ["lv2", "vst3"]
lv2 = ["dep:livi"]
vst3 = ["dep:vst3", "dep:libloading"]

[dependencies]
view = { path = "view" }
livi = { version = "0.7", optional = true }
cpal = "0.15"
midir = "0.10"
crossbeam-channel = "0.5"
clap = { version = "4", features = ["derive"] }
anyhow = "1"
log = "0.4"
env_logger = "0.11"
ratatui = "0.30"
crossterm = "0.28"
toml = "0.8"
toml_edit = "0.22"
serde = { version = "1", features = ["derive"] }
clack-host = { git = "https://github.com/prokopyl/clack" }
clack-finder = { git = "https://github.com/prokopyl/clack" }
clack-extensions = { git = "https://github.com/prokopyl/clack", features = ["clack-host", "audio-ports", "note-ports", "params", "preset-discovery", "state"] }
vst3 = { version = "0.3", optional = true }
libloading = { version = "0.8", optional = true }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[target.'cfg(target_os = "linux")'.dependencies]
cpal = { version = "0.15", features = ["jack"] }

[dev-dependencies]
tempfile = "3"

# lv2-sys upstream (RustAudio/rust-lv2) doesn't compile on macOS — its
# unsupported.rs hits a compile_error! instead of using the Linux bindings.
# PR #117 fixes this with a one-line cfg_attr. Until upstream merges it,
# we pin to akx's fork (1 commit ahead of upstream develop, 0 behind).
[patch.crates-io]
lv2-sys = { git = "https://github.com/akx/rust-lv2", rev = "5e3f2c25c89ba73ccc5e557e5fde047101897cdf" }
